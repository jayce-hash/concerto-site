# Concerto | concertocity.com

User-agent: *
Allow: /

# Serverless function endpoints, not pages
Disallow: /.netlify/

# NOTE ON INDEXING
# Pages that should stay out of Google (auth screens and the in-app mobile
# shells) use a "noindex" meta tag rather than a Disallow here.
# A Disallow blocks crawling, which prevents Google from ever reading the
# noindex, so the URL can still be indexed without its content. Allowing the
# crawl is what lets the noindex actually take effect.

Sitemap: https://concertocity.com/sitemap.xml
